[project]
name = "ateam-common-packets-tools"
version = "0.1.0"
description = "Build tools for ateam-common-packets proto plugin and ROS2 msg generation"
requires-python = ">=3.11"
dependencies = [
    "protobuf>=5.0",
    "pytest>=8.0",
]

[tool.uv]
package = false
